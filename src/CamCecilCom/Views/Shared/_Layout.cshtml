<!doctype HTML>
<html>
    <head>
        <meta charset="utf-8" />
        
        <title>Cam Cecil - @ViewBag.Title</title>

        <!-- styles -->
        <link rel="~/css/bootstrap.min.css" />
        <link rel="~/css/main.css" type="text/css" />

        @* Render Custom CSS, if defined by the layout. *@
        @RenderSection("Styles", false);
    </head>
    <body>

        <div ng-app="cam-cecil">

        </div>

        <!-- js -->
        <script src="~/js/bootstrap.min.js" type="text/javascript"></script>
        @RenderSection("Scripts", false);
    </body>
</html>